# Databricks Configuration
# Required: Your Databricks workspace URL
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com

# Required: Personal Access Token for authentication
# Generate at: Workspace Settings > Developer > Access tokens
DATABRICKS_TOKEN=dapi_your_token_here

# Required: SQL Warehouse ID for query execution
# Find at: SQL Warehouses > Your Warehouse > Connection details
DATABRICKS_WAREHOUSE_ID=your_warehouse_id_here

# Optional: Default catalog and schema for queries
DATABRICKS_CATALOG=workspace
DATABRICKS_SCHEMA=f1

# Optional: Query timeout in seconds (default: 120)
QUERY_TIMEOUT_SECONDS=120

# Optional: Maximum rows to return (default: 10000)
MAX_RESULT_ROWS=10000
